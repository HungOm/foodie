<%= form_with(model: order, url:url,local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul class="fixed">
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :food_id,value: params[:food_id]%>

  </div>
    <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, class:"mt-1 px-3 py-2 bg-white border shadow-sm  placeholder-gray-400 focus:outline-none focus:border-sky-500 focus:ring-sky-500 block w-full rounded-md sm:text-sm focus:ring-2"%>
    <%=form.hidden_field :user_id, value:current_user.id %>
  </div>


  <div class="actions">
    <%= form.submit class:"px-3 py-2 text-white bg-blue-600 hover:bg-blue-700 block w-full rounded-md sm:text-sm" %>
  </div>
<% end %>
